# 🔧 Обновления версии v10.1

## Исправления и улучшения

### 1. ✅ Обновлена версия
- **Было:** v10.0
- **Теперь:** v10.1
- **Где:** Экран входа/регистрации

### 2. ✅ Исправлен баг с переключением между входом и регистрацией
- **Проблема:** При переключении между вкладками вход/регистрация оставались заполненные поля
- **Решение:** Функция `toggleAuth()` теперь очищает все поля при переключении
- **Как работает:**
  ```javascript
  function toggleAuth() { 
      // ... переключение видимости ...
      // Очистить все поля при переключении
      $('l-user').value = '';
      $('l-pass').value = '';
      $('r-user').value = '';
      $('r-pass').value = '';
  }
  ```

### 3. ✅ Удалены стикеры
- Удалена функция `openStickerPicker()`
- Удалены все пакеты стикеров
- Удалена кнопка 🎨 из input bar

### 4. ✅ Добавлена панель управления группой для владельцев

#### Где найти:
- Клик на название группы в заголовке
- **ИЛИ** нажмите кнопку ⚙️ (появляется только для владельца)

#### Что есть в панели:
- ✏️ **Изменить название** - Переименовать группу
- 🖼️ **Изменить аватар** - Загрузить новый аватар группы
- 🔗 **Скопировать ссылку приглашения** - Поделиться доступом
- 👥 **Управление членами:**
  - Выдавать/убирать роль администратора
  - Банить пользователей

#### Функции управления членами:
```
[Имя пользователя] (member/admin)
├─ [Сделать админом / Убрать админа]
└─ [🚫 Бан]
```

### 5. ✅ Технические улучшения

#### На сервере (websocket_server.py):
- Добавлена обработка команды `rename_room` - переименование группы
- Добавлена обработка команды `update_room_avatar` - обновление аватара
- Добавлена обработка команды `change_member_role` - выдача/удаление роли админа
- История запроса теперь включает список членов группы (`room_info['members']`)

#### На клиенте (index.html):
- Новый модальный диалог `room-settings-modal` для управления группой
- Функция `openRoomSettings()` - открыть панель управления
- Функция `loadRoomMembers()` - загрузить и отобразить список членов
- Функция `changeRole()` - изменить роль пользователя
- Функция `banMember()` - забанить пользователя
- Функция `openRoomRename()` - переименовать группу
- Функция `openRoomAvatar()` - изменить аватар группы
- Функция `copyRoomLink()` - скопировать ссылку приглашения
- Функция `updateInputState()` - обновлена для показа кнопки управления

---

## 📋 Чек-лист функций

- ✅ Версия обновлена
- ✅ Баг с полями исправлен
- ✅ Стикеры удалены
- ✅ Панель управления группой добавлена
- ✅ Управление членами работает
- ✅ Только владелец видит кнопку управления

---

## 🚀 Как использовать панель управления

1. **Откройте группу**, которой вы владеете
2. **Нажмите** на название группы в заголовке (или кнопку ⚙️)
3. **Выберите** нужное действие:
   - Изменить название
   - Загрузить аватар
   - Управлять членами
   - Скопировать пригласительную ссылку

4. **Управление членами:**
   - Выбери пользователя
   - Нажми "Сделать админом" или "Убрать админа"
   - Или нажми "🚫 Бан" для удаления из группы

---

## 🐛 Известные ограничения

- Аватар группы сохраняется в памяти (при перезагрузке сервера теряется)
- Для сохранения в БД нужно раскомментировать код в `update_room_avatar`
- Переименование работает только если вы владелец

---

**Версия: 10.1**  
**Дата обновления: 17 февраля 2026**
