# 🚀 Развёртывание NeoChat на Render

## Текущая конфигурация

Ваш сервер **уже готов** к развёртыванию на Render! Вот что было настроено:

### ✅ Выполненная конфигурация

1. **render.yaml** - используется для развёртывания на Render
   - Среда: Python
   - Команда запуска: `python websocket_server.py`
   - Переменная PYTHONUNBUFFERED включена для логирования в реальном времени

2. **websocket_server.py** - слушает на:
   - Хост: `0.0.0.0` (принимает все входящие подключения)
   - Порт: переменная окружения `PORT` (автоматически установлена Render)
   - По умолчанию: `5001` (для локальной разработки)

3. **requirements.txt** - зависимости:
   - `websockets==13.0` - основная библиотека WebSocket
   - `aiohttp==3.9.1` - для асинхронных HTTP запросов
   - `gunicorn==21.2.0` - для дополнительной надёжности

4. **runtime.txt** - версия Python:
   - `python-3.11.7` - совместимо с Render

## Как развернуть на Render

### Шаг 1: Подготовьте репозиторий на GitHub
```bash
git init
git add .
git commit -m "Initial commit: NeoChat server"
git remote add origin <URL вашего GitHub репозитория>
git push -u origin main
```

### Шаг 2: На сайте Render (https://render.com)

1. **Авторизуйтесь** через GitHub
2. **Создайте новый Web Service**
   - Выберите ваш репозиторий NeoChat
   - Нажмите "Connect"
3. **Заполните конфигурацию**:
   - **Name**: `neochat-server`
   - **Runtime**: `python`
   - **Build Command**: `pip install -r requirements.txt`
   - **Start Command**: `python websocket_server.py`
   - **Plan**: Free (или Premium для лучшего периодично)

4. **Нажмите "Deploy"**

### Шаг 3: Получите URL вашего сервера

После развёртывания Render предоставит вам URL типа:
```
https://neochat-server-b1jq.onrender.com
```

Для WebSocket подключения используйте:
```
wss://neochat-server-b1jq.onrender.com
```
(Render автоматически преобразует HTTPS в WSS для WebSocket)

## Важные замечания

### 🔐 Безопасность
- Используется `wss://` (WebSocket Secure) - зашифрованное соединение
- Все данные передаются защифрованно

### 💾 Хранилище данных
- **На бесплатном плане Render**: база данных будет сброшена при перезагрузке контейнера (каждые 15 минут неактивности)
- **Рекомендация**: Используйте Render PostgreSQL для постоянного хранилища (платная опция)

### 📊 Логирование
- Все логи доступны в панели Render: **Logs** → вкладка **stdout**
- Используются эмодзи для удобства отслеживания событий

### 🌐 Frontend подключение
В вашем `index.html` или клиентском коде используйте:
```javascript
const WS_URL = 'wss://neochat-server-b1jq.onrender.com';
const ws = new WebSocket(WS_URL);
```

## Статус сервера

Для проверки статуса откройте:
```
https://neochat-server-b1jq.onrender.com
```

Вы должны увидеть сообщение об ошибке (т.к. WebSocket сервер не отвечает на HTTP), но это указывает, что сервер запущен.

## Оптимизация

### Для лучшей производительности:
1. **Используйте собственный домен** (Premium план)
2. **Включите HTTP keep-alive** в client коде
3. **Установите максимальный размер сообщения** (уже установлено на 20 MB)

### Для лучшей надёжности:
1. **Перейдите на Premium план** (постоянное хранилище)
2. **Подключите PostgreSQL базу** вместо SQLite
3. **Включите monitoring** в панели Render

## Решение проблем

### Сервер не запускается:
- Проверьте логи в панели Render
- Убедитесь, что все файлы правильно загружены
- Проверьте наличие `requirements.txt` и `runtime.txt`

### Медленное подключение:
- На бесплатном плане возможны задержки
- Перейдите на Premium для лучшей производительности

### Потеря данных:
- Это нормально для бесплатного плана Render
- Используйте PostgreSQL для постоянного хранилища

## Ссылка на ваш сервер

🎉 **Ваш сервер доступен по адресу:**
```
https://neochat-server-b1jq.onrender.com
```

**WebSocket URL для клиента:**
```
wss://neochat-server-b1jq.onrender.com
```

Good luck! 🚀
